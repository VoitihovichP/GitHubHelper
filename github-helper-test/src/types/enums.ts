export enum BannerTitles {
  START_SEARCHING = 'Start with searching a GitHub user',
  USER_NOT_FOUND = 'User not found',
  REPO_NOT_FOUND = 'Repository list is empty',
}

export enum ActionsTypes {
  ADD_USER_INFO = 'ADD_USER_INFO',
  ADD_REPO_INFO = 'ADD_REPO_INFO',
}
